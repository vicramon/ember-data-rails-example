h3 Story List
each story in  controller
  = render 'story' story

h2 Modals

= linkTo 'left_slide' | Route-based modal

= outlet

br
br

a href="#" click="openCenterModal" Global modal

= outlet 'center_modal'
